/**
 * TilemapLit component marker - when attached to a TileMap entity,
 * the tilemap will be rendered with lighting.
 *
 * Usage:
 * ```typescript
 * commands.spawn(tilemap, new TilemapLit());
 * ```
 */
export class TilemapLit {
  // Marker component - no data needed
}
